<script setup>
// Import defineProps from Vue for defining component props
import { defineProps } from 'vue';

// Define 'title' and 'subtitle' props with default values
defineProps({
  title: {
    type: String,
    default: 'Become a Vue Dev',
  },
  subtitle: {
    type: String,
    default: 'Find the Vue job that fits your skills and needs',
  }
});
</script>
<template>
  <!--
    Hero section with a green background, centered content, and large heading.
    Displays the 'title' and 'subtitle' props.
  -->
  <section class="bg-green-700 py-20 mb-4">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 flex flex-col items-center">
      <div class="text-center">
        <h1 class="text-4xl font-extrabold text-white sm:text-5xl md:text-6xl">
          {{ title }}
        </h1>
        <p class="my-4 text-xl text-white">
          {{ subtitle }}
        </p>
      </div>
    </div>
  </section>
</template>
